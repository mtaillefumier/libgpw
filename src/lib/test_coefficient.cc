#include <iostream>
#include <algorithm>
#include <string>
#include <cstdio>
#include <cstring>
#include <cmath>
#include <chrono>
#include <vector>

#include "coefficients.hpp"

int main(int argc, char **argv)
{
		int lmin[2], lmax[2];


		compute_compact_polynomial_coefficients(const mdarray<T, 2, CblasRowMajor> &coef,
																						const int lmin[2], const int lmax[2],
																						const T *ra, const T *rb, const T *rab,
																						mdarray<T, 3, CblasRowMajor> &co)
}
